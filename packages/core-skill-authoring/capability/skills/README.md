# Skill-authoring skills

Place each authoring skill in `skills/<skill-id>/SKILL.md`. Keep optional
`references/`, `scripts/`, `examples/`, `templates/`, and other reusable assets
inside the same skill directory.

This README keeps the empty scaffold in version control and is not materialized
as a skill.
