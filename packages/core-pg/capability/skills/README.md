# PostgreSQL skills

Place each PostgreSQL skill in `skills/<skill-id>/SKILL.md`. Keep optional
`references/`, `scripts/`, `examples/`, and `templates/` inside the same skill
directory.

This README keeps the empty scaffold in version control and is not materialized
as a skill.
